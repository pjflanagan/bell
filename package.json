{
  "name": "bell-monorepo",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "vscode": "^1.110.0"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm test --workspaces"
  }
}
