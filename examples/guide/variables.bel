# Assign a string to a variable
var apiKey = "my-secret-token"
var userId = 42

# Use variables inside strings with {variableName}
url "https://httpbin.org/get?id={userId}"
header "Authorization" "Bearer {apiKey}"

GET
