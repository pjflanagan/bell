url "https://httpbin.org/post"

body {
  "username": "testuser",
  "password": "secret",
  "token": "fake-token",
  "userId": 42
}

POST

# Save values from the response
token = response.body.json.token
userId = response.body.json.userId

# export makes these variables available to files that request this one
export token, userId
