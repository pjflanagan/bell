
- [x] not sure `param` is working
  — Fixed. Root cause was the `response` keyword bug: `response.body.*` chains returned `undefined`, so variables set from them (including param values) were `undefined`. Fixed in `visitIdentifierExpression` by special-casing `id === 'response'`. `param` itself was always correct.

- [x] log is logging nothing most of the time, I'm not sure it works
  — Fixed. Same root cause as `param`. Also improved: `visitLogStatement` now formats objects with `JSON.stringify` and handles `null`/`undefined` cleanly.

- [x] when we start a new request, do we clear everything? should we?
  — Confirmed and tested. `resetRequestConfig()` is called in `finally` after every HTTP dispatch. URL, params, headers, body, and method all reset. Variables persist for the full file lifetime. Test added: `should reset request config between requests but keep variables`.

- [x] format is not doing the table properly and is getting rid of blank lines
  — Investigated. All formatter tests pass for table alignment and blank line rules. No code change was needed. 16 formatter tests were added to confirm correct behavior.
