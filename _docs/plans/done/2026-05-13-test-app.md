
There is an app in apps/test-app that currently has nothing in it.

It should be a simple Bun server with a few api routes that we can serve locally and test bell files on manually.

It can also serve as an example of how Bell looks when installed in a project.