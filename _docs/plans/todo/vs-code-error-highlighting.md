
## VS Code Error Highlighting

We need error highlighting on bell files.
Enhance VS Code extension with IntelliSense and autocomplete.

For example:
- if a user writes a command that does exist
- if a user fills out in an unexpected format
- if a variable is not defined or `require`d
- ...